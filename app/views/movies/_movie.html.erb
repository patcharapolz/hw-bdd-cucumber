<% @movies.each do |movie| %>
    <tr>
    <td>
        <%= movie.title %>
        <%= movie.rating %>
        <%= movie.release_date %>
        <%= link_to "More about #{movie.title}", movie_path(movie) %>
    </td>
    </tr>
<% end %>